version: "3.8"

services:
  puglla:
    image: ghcr.io/andrespuglla5655/recuperacion:puglla-3.0.0
    deploy:
      replicas: 1
      labels:
        - "traefik.enable=true"
        - "traefik.http.routers.puglla.entrypoints=http"
        - "traefik.http.routers.puglla.rule=Host(`byron.byronrm.com`)"
        - "traefik.http.middlewares.puglla-https-redirect.redirectscheme.scheme=https"
        - "traefik.http.routers.puglla.middlewares=puglla-https-redirect"
        - "traefik.http.routers.puglla-secure.entrypoints=https"
        - "traefik.http.routers.puglla-secure.rule=Host(`byron.byronrm.com`)"
        - "traefik.http.routers.puglla-secure.tls=true"
        - "traefik.http.routers.puglla-secure.tls.certresolver=http"
        - "traefik.http.routers.puglla-secure.service=puglla"
        - "traefik.http.services.puglla.loadbalancer.server.port=5000"
        - "traefik.docker.network=traefik-public"
      restart_policy:
        condition: on-failure
        delay: 5s
        max_attempts: 3
    networks:
      - traefik-public
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

networks:
  traefik-public:
    external: true